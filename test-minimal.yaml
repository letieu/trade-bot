telegram:
  botToken: "test_token"
  chatId: "test_chat"

bybit:
  baseUrl: "https://api.bybit.com"
  timeout: "10s"
  rateLimit: 20

bot:
  scanInterval: "30s"
  batchSize: 10
  maxConcurrency: 3
  enabledIntervals:
    - "5m"
    - "15m"